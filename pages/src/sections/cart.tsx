import { VStack } from "@chakra-ui/react";

const Cart = () => {
  return (
    <VStack
      width="full"
      height="full"
      padding={10}
      spacing={10}
      alignItems="flex-start"
      background={"gray.50"}
    ></VStack>
  );
};

export default Cart;
